{
  "name": "aib-mobility",
  "version": "0.5.0",
  "scripts": {
    "prebuild": "rm -rf dist-form",
    "build": "mkdir dist-form && cat polyfill.js > dist-form/calculate.js && cat price-form.js >> dist-form/calculate.js"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "jquery": "^3.6.0",
    "luxon": "^2.0.2"
  },
  "engines": {
    "node": ">= 12"
  }
}
