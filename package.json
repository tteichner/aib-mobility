{
  "name": "aib-mobility",
  "version": "0.5.0",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "mkdir dist && cat polyfill.js > dist/calculate.js && cat price-form.js >> dist/calculate.js"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "jquery": "^3.6.0",
    "luxon": "^2.0.2"
  },
  "engines": {
    "node": ">= 12"
  }
}
